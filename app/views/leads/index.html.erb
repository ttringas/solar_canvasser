<h1>My Leads</h1>
<ul>
  <% @leads.each do |lead| %>
    <li><%= link_to lead.name, lead_path(lead) %></li>
  <% end %>
</ul>

<%= link_to "Dashboard", root_path, class: "btn btn-large btn-default" %>